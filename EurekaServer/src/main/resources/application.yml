server:
  port: 8761 #注册中心服务端口默认为8761
eureka:
  client:  #服务中心自身也是一个client
    register-with-eureka: false   #是否要注册自己
    fetch-registry: false  #是否要同步其他注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/ #访问注册中心的路径
